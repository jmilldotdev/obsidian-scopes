import * as React from "react";

import ScopesPlugin from "../main";

const SettingsForm = ({ plugin }: { plugin: ScopesPlugin }) => {
	return <div className="settingsForm">Settings</div>;
};

export default SettingsForm;
